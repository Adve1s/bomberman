package com.bomberman.bomberman.shared.network;

/**
 * Messages the server sends to clients (state updates, events, handshake replies).
 */
public interface ServerToClientMessage extends NetworkMessage {}